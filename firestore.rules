rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all read and write operations for all documents and collections
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

